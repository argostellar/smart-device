'use strict';

(function () {
  const KeyboardCode = {
    ESC: 27,
    ENTER: 13
  };

  const BtnState = {
    SHOW: 'main-footer__btn--show',
    HIDE: 'main-footer__btn--hide'
  };

  const SimilarityState = {
    SIMILAR: 1,
    NONSIMILAR: -1
  };

})();
